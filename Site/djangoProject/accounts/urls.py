'''from django.urls import path
from .views import signup, login_view, add_controller, home

urlpatterns = [
    path('signup/', signup, name='signup'),
    path('login/', login_view, name='login'),
    path('add_controller/', add_controller, name='add_controller'),
    path('', home, name='home'),
]'''